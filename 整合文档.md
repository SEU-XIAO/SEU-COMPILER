# 编译原理实验项目整合文档

## 目录
1. [项目概述](#项目概述)
2. [整体架构设计](#整体架构设计)
3. [模块整合方案](#模块整合方案)
4. [整合后项目结构](#整合后项目结构)
5. [接口定义与数据格式](#接口定义与数据格式)
6. [编译与运行流程](#编译与运行流程)
7. [代码优化与重构建议](#代码优化与重构建议)
8. [未来扩展方向](#未来扩展方向)

---

## 1. 项目概述

本实验包含三个独立的编译器前端模块：

| 模块 | 功能 | 当前状态 |
|-----|------|---------|
| **MySEULex** | 词法分析器生成器 | ✅ 已完成 |
| **SeuYacc** | 语法分析器生成器 | ✅ 已完成 |
| **中间代码生成器** | LLVM IR 生成器 | ✅ 已完成 |

**整合目标**：将三个模块整合成一个完整的 C99 编译器前端，实现从 C 源代码到 LLVM IR 的完整编译流程。

---

## 2. 整体架构设计

### 2.1 编译器前端架构

```
┌─────────────────────────────────────────────────────────────────┐
│                    C99 编译器前端整体架构                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│   源代码 (.c)                                                   │
│        │                                                        │
│        ▼                                                        │
│   ┌──────────────┐                                              │
│   │  词法分析器   │  MySEULex 生成                               │
│   │  (Lexer)     │                                              │
│   └──────┬───────┘                                              │
│          │  Token 流                                            │
│          ▼                                                      │
│   ┌──────────────┐                                              │
│   │  语法分析器   │  SeuYacc 生成                                │
│   │  (Parser)    │                                              │
│   └──────┬───────┘                                              │
│          │  抽象语法树 (AST)                                     │
│          ▼                                                      │
│   ┌──────────────┐                                              │
│   │  中间代码生成器 │  LLVM IR Generation                        │
│   │  (CodeGen)   │                                              │
│   └──────┬───────┘                                              │
│          │  LLVM IR                                              │
│          ▼                                                      │
│   目标代码 (.obj/.exe)  ← LLVM 后端                              │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 模块交互流程

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│  MySEULex   │      │   SeuYacc   │      │ CodeGen     │
│  (词法生成)  │      │  (语法生成)  │      │ (中间代码)   │
└──────┬──────┘      └──────┬──────┘      └──────┬──────┘
       │                    │                    │
       ▼                    ▼                    ▼
  .l 文件 ──────┐      .y 文件 ──────┐     AST 节点
       │        │           │        │          │
       │        ▼           │        ▼          │
       │   Lexer.cpp        │    Parser.cpp     │
       │        │           │        │          │
       │        ▼           │        ▼          │
       │   Token 流 ────────┼───────→ 语法分析   │
       │                    │        │          │
       │                    │        ▼          │
       │                    │     AST 树 ──────→│
       │                    │                    │
       └────────────────────┴────────────────────┘
                              │
                              ▼
                         LLVM IR
```

---

## 3. 模块整合方案

### 3.1 整合策略

| 策略 | 描述 |
|-----|------|
| **管道式整合** | 词法分析 → 语法分析 → 中间代码生成，数据逐级传递 |
| **统一接口** | 定义标准的 Token 格式和 AST 接口 |
| **模块化设计** | 保持各模块独立性，通过接口交互 |
| **构建自动化** | 使用 Makefile 统一管理编译流程 |

### 3.2 整合步骤

#### 步骤 1：统一 Token 格式

定义标准的 Token 结构（在 `common/token.h` 中）：

```cpp
struct Token {
    enum Type {
        // 关键字
        TOK_INT, TOK_VOID, TOK_IF, TOK_ELSE, TOK_WHILE, TOK_FOR,
        TOK_RETURN, TOK_BREAK, TOK_CONTINUE, TOK_DO,
        
        // 标识符和常量
        TOK_IDENTIFIER, TOK_NUMBER, TOK_FLOAT, TOK_CHAR, TOK_STRING,
        
        // 运算符
        TOK_PLUS, TOK_MINUS, TOK_MUL, TOK_DIV, TOK_MOD,
        TOK_ASSIGN, TOK_EQ, TOK_NE, TOK_LT, TOK_GT, TOK_LE, TOK_GE,
        TOK_AND, TOK_OR, TOK_NOT, TOK_BITAND, TOK_BITOR, TOK_XOR,
        TOK_SHL, TOK_SHR,
        
        // 标点符号
        TOK_LPAREN, TOK_RPAREN, TOK_LBRACE, TOK_RBRACE,
        TOK_LBRACKET, TOK_RBRACKET, TOK_SEMICOLON, TOK_COMMA,
        TOK_DOT, TOK_ARROW, TOK_ELLIPSIS,
        
        TOK_EOF
    } type;
    
    std::string value;    // Token 值（如标识符名称、常量值）
    int line;             // 行号
    int column;           // 列号
};
```

#### 步骤 2：统一 AST 接口

使用现有的 `intermediate-code-generation/ast.h` 作为统一的 AST 定义。

#### 步骤 3：连接词法分析与语法分析

修改语法分析器，使其能够接收词法分析器的 Token 流：

```cpp
// lexer.h
class Lexer {
public:
    Lexer(const std::string& filename);
    Token getNextToken();
    bool hasMoreTokens();
};

// parser.h
class Parser {
public:
    Parser(Lexer& lexer);
    ASTNode* parse();
};
```

#### 步骤 4：连接语法分析与中间代码生成

语法分析器输出 AST，中间代码生成器接收 AST 并生成 LLVM IR：

```cpp
// codegen.h
class CodeGenerator {
public:
    CodeGenerator();
    llvm::Module* generate(ASTNode* ast);
    void printIR(const std::string& filename);
};
```

---

## 4. 整合后项目结构

```
SEU-COMPILER/
├── README.md                    # 项目说明文档
├── Makefile                     # 统一构建脚本
├── bin/                         # 可执行文件输出目录
│   └── seucc                    # 编译器主程序
├── src/                         # 源代码目录
│   ├── common/                  # 公共模块
│   │   ├── token.h              # Token 定义
│   │   ├── token.cpp
│   │   ├── utils.h              # 工具函数
│   │   └── utils.cpp
│   ├── lexer/                   # 词法分析模块
│   │   ├── lexer.h
│   │   ├── lexer.cpp
│   │   └── c99.l                # 词法规则
│   ├── parser/                  # 语法分析模块
│   │   ├── parser.h
│   │   ├── parser.cpp
│   │   └── c99.y                # 语法规则
│   ├── ast/                     # AST 模块
│   │   ├── ast.h
│   │   └── ast.cpp
│   ├── codegen/                 # 代码生成模块
│   │   ├── codegen.h
│   │   └── codegen.cpp
│   └── main.cpp                 # 主函数
├── test/                        # 测试目录
│   ├── test1.c                  # 测试用例
│   ├── test2.c
│   └── expected/                # 期望输出
└── doc/                         # 文档目录
    ├── 测试文档.md
    └── 整合文档.md
```

---

## 5. 接口定义与数据格式

### 5.1 Token 流格式

词法分析器输出的 Token 流格式：

| 字段 | 类型 | 说明 |
|-----|------|------|
| type | Token::Type | Token 类型枚举 |
| value | std::string | Token 的字符串表示 |
| line | int | 源代码行号 |
| column | int | 源代码列号 |

### 5.2 AST 节点类型

参考 `intermediate-code-generation/ast.h` 中的 ASTType 枚举。

### 5.3 LLVM IR 输出格式

生成标准的 LLVM IR 文本格式，可直接用于 LLVM 后端编译。

---

## 6. 编译与运行流程

### 6.1 编译流程

```bash
# 进入项目目录
cd SEU-COMPILER

# 清理旧构建
make clean

# 编译整个项目
make

# 输出: bin/seucc (编译器可执行文件)
```

### 6.2 Makefile 设计

```makefile
# 编译器配置
CXX = g++
CXXFLAGS = -std=c++17 -Wall -O2 `llvm-config --cxxflags`
LDFLAGS = `llvm-config --ldflags --libs core`

# 目标文件
TARGET = bin/seucc

# 源文件
SRCS = src/main.cpp \
       src/common/utils.cpp \
       src/lexer/lexer.cpp \
       src/parser/parser.cpp \
       src/ast/ast.cpp \
       src/codegen/codegen.cpp

# Flex/Bison 生成文件
LEX_SRC = src/lexer/c99.l
YACC_SRC = src/parser/c99.y
LEX_OUT = src/lexer/lex.yy.cpp
YACC_OUT = src/parser/c99.tab.cpp

.PHONY: all clean

all: $(TARGET)

# 生成词法分析器
$(LEX_OUT): $(LEX_SRC)
	flex -o $@ $<

# 生成语法分析器
$(YACC_OUT): $(YACC_SRC)
	bison -d -o $@ $<

# 编译主程序
$(TARGET): $(SRCS) $(LEX_OUT) $(YACC_OUT)
	@mkdir -p bin
	$(CXX) $(CXXFLAGS) -o $@ $^ $(LDFLAGS)

clean:
	rm -f $(TARGET) $(LEX_OUT) $(YACC_OUT) src/parser/c99.tab.h
```

### 6.3 运行流程

```bash
# 使用编译器编译 C 代码
./bin/seucc test/test1.c -o output.ll

# 查看生成的 LLVM IR
cat output.ll

# 使用 LLVM 进一步编译为可执行文件
llc output.ll -o output.s
gcc output.s -o output.exe

# 运行生成的程序
./output.exe
```

---

## 7. 代码优化与重构建议

### 7.1 现有问题修复

| 问题 | 位置 | 修复方案 |
|-----|------|---------|
| `dumpProducers` 类型不匹配 | `SeuYacc/main.cpp:19` | 使用正确的函数指针类型 |
| 重复变量定义 | `yaccSource.cpp:143-145` | 删除重复定义 |
| 中文注释编码问题 | `yaccUtil.h` | 改为英文注释或确保 UTF-8 编码 |

### 7.2 代码重构建议

#### 建议 1：使用智能指针管理内存

```cpp
// 重构前
ASTNode* node = new ASTNode(AST_VAR);

// 重构后
std::unique_ptr<ASTNode> node = std::make_unique<ASTNode>(AST_VAR);
```

#### 建议 2：添加命名空间

```cpp
namespace seucc {
    namespace lexer { /* ... */ }
    namespace parser { /* ... */ }
    namespace codegen { /* ... */ }
}
```

#### 建议 3：使用现代 C++ 特性

- 使用 `std::variant` 替代联合体
- 使用范围 for 循环
- 使用 `constexpr` 常量

### 7.3 性能优化

| 优化项 | 说明 |
|-------|------|
| Token 池 | 复用 Token 对象，减少内存分配 |
| AST 内存池 | 批量分配 AST 节点内存 |
| 符号表优化 | 使用哈希表替代线性查找 |

---

## 8. 未来扩展方向

### 8.1 功能扩展

| 方向 | 描述 |
|-----|------|
| **语义分析** | 添加类型检查、作用域分析 |
| **代码优化** | 实现基本的优化 pass |
| **目标代码生成** | 支持 x86-64、ARM 等目标架构 |
| **调试信息** | 生成 DWARF 调试信息 |

### 8.2 架构扩展

| 方向 | 描述 |
|-----|------|
| **插件系统** | 支持自定义优化 pass |
| **模块化设计** | 将各阶段设计为独立模块 |
| **并行编译** | 支持多文件并行编译 |

### 8.3 兼容性扩展

| 方向 | 描述 |
|-----|------|
| **C++ 支持** | 扩展语法支持 C++ |
| **标准库支持** | 集成 C 标准库 |
| **跨平台支持** | 支持 Windows/Linux/macOS |

---

## 附录：整合检查清单

| 整合项 | 状态 | 备注 |
|-------|------|------|
| 统一 Token 格式 | ☐ | 定义标准 Token 结构 |
| 统一 AST 接口 | ☐ | 使用现有 ast.h |
| 词法-语法连接 | ☐ | 实现 Lexer-Parser 接口 |
| 语法-代码生成连接 | ☐ | 实现 AST-CodeGen 接口 |
| 统一 Makefile | ☐ | 管理整个项目编译 |
| 测试用例覆盖 | ☐ | 编写整合测试 |

---

**文档版本**: v1.0  
**创建日期**: 2026年5月  
**作者**: 杨熙锐