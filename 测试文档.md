# 编译原理实验项目测试文档

## 目录
1. [项目概述](#项目概述)
2. [测试环境准备](#测试环境准备)
3. [MySEULex 词法分析器生成器测试](#myseulex-词法分析器生成器测试)
4. [SeuYacc 语法分析器生成器测试](#seuyacc-语法分析器生成器测试)
5. [中间代码生成器测试](#中间代码生成器测试)
6. [测试结果汇总](#测试结果汇总)

---

## 1. 项目概述

本实验包含三个主要项目模块：

| 项目 | 功能 | 位置 |
|-----|------|------|
| **MySEULex** | 词法分析器生成器，将 `.l` 文件转换为词法分析器 | `71123331-杨熙锐-MySEULex/` |
| **SeuYacc** | 语法分析器生成器，将 `.y` 文件转换为语法分析器 | `SeuYacc/` |
| **中间代码生成器** | 基于 Flex/Bison/LLVM 的中间代码生成器 | `intermediate-code-generation/` |

---

## 2. 测试环境准备

### 2.1 依赖工具安装

#### Windows 环境

1. **MinGW-w64**（提供 g++/make）
   - 下载地址：https://sourceforge.net/projects/mingw-w64/
   - 安装路径建议：`C:\mingw-w64`
   - 添加 `C:\mingw-w64\x86_64-8.1.0-posix-seh-rt_v6-rev0\mingw64\bin` 到系统 PATH

2. **Flex/Bison for Windows**
   - 下载地址：https://sourceforge.net/projects/winflexbison/
   - 解压到 `C:\win_flex_bison`
   - 添加到系统 PATH

3. **LLVM 18.x**（用于中间代码生成）
   - 下载地址：https://github.com/llvm/llvm-project/releases
   - 安装路径：`C:\Program Files\LLVM`
   - 添加 `C:\Program Files\LLVM\bin` 到系统 PATH

#### Linux/macOS 环境

```bash
# Ubuntu/Debian
sudo apt update
sudo apt install gcc g++ make flex bison llvm-18 llvm-18-dev

# macOS (Homebrew)
brew install gcc make flex bison llvm@18
```

### 2.2 环境验证

打开终端执行以下命令验证安装：

```bash
g++ --version    # 应显示 g++ 版本信息
make --version   # 应显示 make 版本信息
flex --version   # 应显示 flex 版本信息
bison --version  # 应显示 bison 版本信息
llvm-config --version  # 应显示 LLVM 版本（如 18.1.8）
```

---

## 3. MySEULex 词法分析器生成器测试

### 3.1 测试目标

验证 MySEULex 能够：
- 正确解析 `.l` 词法规则文件
- 生成有效的词法分析器代码
- 正确识别 C99 语言的各种 token

### 3.2 测试步骤

#### 步骤 1：编译 MySEULex 生成器

```bash
cd "7112331-杨熙锐-MySEULex/2-词法分析程序生成器源程序"
make clean
make
```

#### 步骤 2：生成词法分析器

```bash
# 使用 complete_c99.l 生成词法分析器
./seuLex "../1-Lex输入文件/complete_c99.l" "../3-生成的词法分析程序/complete_c99_lex.yy.cpp"
```

#### 步骤 3：编译生成的词法分析器

```bash
g++ -std=c++17 -o c99_lexer "../3-生成的词法分析程序/complete_c99_lex.yy.cpp"
```

#### 步骤 4：运行测试

```bash
# 测试基本 C 代码
./c99_lexer < "../4-测试用例和测试结果/test_input.c"

# 测试 C99 综合特性
./c99_lexer < "../4-测试用例和测试结果/test_c99_input.c"
```

### 3.3 预期输出

词法分析器应输出类似以下格式的 token 序列：

```
TOK_INT
TOK_IDENTIFIER:main
TOK_LPAREN
TOK_RPAREN
TOK_LBRACE
TOK_INT
TOK_IDENTIFIER:a
TOK_ASSIGN
TOK_NUMBER:10
TOK_SEMICOLON
...
```

### 3.4 测试用例

| 测试用例 | 路径 | 测试内容 |
|---------|------|---------|
| test_input.c | `4-测试用例和测试结果/test_input.c` | 基本变量声明、赋值、条件语句 |
| test_c99_input.c | `4-测试用例和测试结果/test_c99_input.c` | C99 综合特性（数组、字符串、注释、科学记数法） |

---

## 4. SeuYacc 语法分析器生成器测试

### 4.1 测试目标

验证 SeuYacc 能够：
- 正确解析 `.y` 语法规则文件
- 生成 LALR 语法分析表
- 生成有效的语法分析器代码

### 4.2 测试步骤

#### 步骤 1：编译 SeuYacc 生成器

```bash
cd SeuYacc
make clean
make
```

#### 步骤 2：生成语法分析器

```bash
./seuYacc
```

执行后应生成以下文件：
- `y.tab.h` - 语法分析器头文件
- `y.tab.c` - 语法分析器实现文件

#### 步骤 3：编译生成的语法分析器

```bash
gcc -std=c99 -o parser y.tab.c
```

#### 步骤 4：准备测试输入

创建测试输入文件 `test.lo`（词法分析器输出格式）：

```
300 < 1 , 1 > int
301 < 1 , 5 > main
302 < 1 , 10 > (
303 < 1 , 11 > )
304 < 1 , 12 > {
300 < 2 , 5 > int
301 < 2 , 9 > a
305 < 2 , 11 > =
306 < 2 , 13 > 10
307 < 2 , 15 > ;
308 < 3 , 5 > return
301 < 3 , 12 > a
307 < 3 , 13 > ;
309 < 4 , 1 > }
```

#### 步骤 5：运行测试

```bash
./parser test.lo output.txt
```

### 4.3 预期输出

输出文件应包含语法树的层次结构表示：

```
__PROGRAM__ : => program
 program : => declaration_list declaration
  declaration_list : => declaration
   declaration : => type_specifier declarator ;
    type_specifier : => int
    declarator : => IDENTIFIER
   declaration : => type_specifier declarator ;
    type_specifier : => int
    declarator : => IDENTIFIER ( ) compound_statement
     compound_statement : => { declaration_list statement_list }
      declaration_list : => declaration
       declaration : => type_specifier init_declarator_list ;
        type_specifier : => int
        init_declarator_list : => init_declarator
         init_declarator : => declarator = initializer
          declarator : => IDENTIFIER
          initializer : => CONSTANT
      statement_list : => statement
       statement : => return expression ;
        expression : => IDENTIFIER
```

---

## 5. 中间代码生成器测试

### 5.1 测试目标

验证中间代码生成器能够：
- 正确解析 C 语言源代码
- 构建抽象语法树（AST）
- 生成有效的 LLVM IR 代码

### 5.2 测试步骤

#### 步骤 1：生成词法/语法分析器

```bash
cd intermediate-code-generation
make clean
make
```

#### 步骤 2：准备测试输入

使用项目中的 `test.c` 文件：

```c
int main() {
    int a = 10;
    int b = 20;
    int c = a + b;
    return c;
}
```

#### 步骤 3：运行测试

```bash
./compiler test.c
```

### 5.3 预期输出

应输出类似以下的 LLVM IR 代码：

```llvm
; ModuleID = 'c99 compiler'
source_filename = "c99 compiler"

define i32 @main() {
entry:
  %a = alloca i32, align 4
  store i32 10, ptr %a, align 4
  %b = alloca i32, align 4
  store i32 20, ptr %b, align 4
  %c = alloca i32, align 4
  %a.val = load i32, ptr %a, align 4
  %b.val = load i32, ptr %b, align 4
  %addtmp = add i32 %a.val, %b.val
  store i32 %addtmp, ptr %c, align 4
  %c.val = load i32, ptr %c, align 4
  ret i32 %c.val
}
```

---

## 6. 测试结果汇总

### 测试检查表

| 测试项 | 状态 | 备注 |
|-------|------|------|
| MySEULex 编译通过 | ☐ | SeuLex 生成器编译 |
| MySEULex 生成词法分析器 | ☐ | 使用 complete_c99.l |
| 词法分析器测试通过 | ☐ | 识别所有 token |
| SeuYacc 编译通过 | ☐ | SeuYacc 生成器编译 |
| SeuYacc 生成语法分析器 | ☐ | 生成 y.tab.h/y.tab.c |
| 语法分析器测试通过 | ☐ | 正确解析语法 |
| 中间代码生成器编译通过 | ☐ | 依赖 LLVM |
| LLVM IR 生成正确 | ☐ | 生成有效中间代码 |

### 测试问题记录

| 问题编号 | 问题描述 | 严重程度 | 状态 |
|---------|---------|---------|------|
| TST-001 | | | |
| TST-002 | | | |
| TST-003 | | | |

---

## 附录：测试脚本

### 完整测试脚本（Linux/macOS）

```bash
#!/bin/bash

echo "=== 编译原理实验项目测试 ==="

# 测试 MySEULex
echo -e "\n[1/3] 测试 MySEULex..."
cd "7112331-杨熙锐-MySEULex/2-词法分析程序生成器源程序"
make clean > /dev/null 2>&1
make > /dev/null 2>&1
if [ $? -eq 0 ]; then
    echo "✓ MySEULex 编译成功"
else
    echo "✗ MySEULex 编译失败"
    exit 1
fi

# 测试 SeuYacc
echo -e "\n[2/3] 测试 SeuYacc..."
cd ../../SeuYacc
make clean > /dev/null 2>&1
make > /dev/null 2>&1
if [ $? -eq 0 ]; then
    echo "✓ SeuYacc 编译成功"
else
    echo "✗ SeuYacc 编译失败"
    exit 1
fi

# 测试中间代码生成器
echo -e "\n[3/3] 测试中间代码生成器..."
cd ../intermediate-code-generation
make clean > /dev/null 2>&1
make > /dev/null 2>&1
if [ $? -eq 0 ]; then
    echo "✓ 中间代码生成器编译成功"
else
    echo "✗ 中间代码生成器编译失败"
    exit 1
fi

echo -e "\n=== 所有测试完成 ==="
```

---

**文档版本**: v1.0  
**创建日期**: 2026年5月  
**作者**: 杨熙锐